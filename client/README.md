# Client (frontend)

This folder will contain the frontend React app (Vite + TypeScript + Tailwind).

After moving the frontend files into `client/`, use the usual commands inside the `client/` folder:

Install dependencies:

```bash
npm install
```

Start dev server:

```bash
npm run dev
```
